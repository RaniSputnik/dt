/// terrain_get_width(terrain)
///
/// Gets the width of the given terrain in room dimensions
/// @param terrain The terrain to get the size of
/// @return The width of the terrain in pixels

return argument0.width * argument0.scale;
